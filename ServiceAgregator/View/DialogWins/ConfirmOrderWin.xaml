<Window x:Class="ServiceAgregator.View.DialogWins.ConfirmOrderWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"     
        xmlns:local="clr-namespace:ServiceAgregator.View.DialogWins"
        mc:Ignorable="d"
        Title="Confirm Order" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
        </Style>
        
    </Window.Resources>

    <Grid Background="#cfdaec">      
        <StackPanel Margin="20">
            <TextBlock>Заказ оформлен верно?</TextBlock>
            <TextBlock>Описание заказ</TextBlock>
            <TextBlock Text="{Binding Path=Services.Description}"></TextBlock>
            <TextBlock>Дата выполнения заказа</TextBlock>
            <TextBlock Text="{Binding Path=Date_Execution}"></TextBlock>
            <WrapPanel HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button IsDefault="True" Click="Button_Click_1" MinWidth="60" Margin="0,0,10,0">Да</Button>
                <Button IsCancel="True" MinWidth="60">Отмена</Button>
            </WrapPanel>
        </StackPanel>  
    </Grid>
</Window>
